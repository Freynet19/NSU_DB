SQL-файлы в `db/init/` извлечены из `Отчёт 23201 Смирнов версия 0.6.md` скриптом разбиения по разделам.

`06_prepare_only.sql` — только операторы `PREPARE` (без примеров `EXECUTE`) для загрузки в сессию Qt-клиента.
